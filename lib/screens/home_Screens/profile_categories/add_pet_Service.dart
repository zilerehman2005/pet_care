import 'package:flutter/material.dart';

class AddPetService extends StatelessWidget {
  const AddPetService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add pet service"),
      ),
    );
  }
}
